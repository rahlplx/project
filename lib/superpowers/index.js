/**
 * Superpowers Module
 * TDD workflow and subagent dispatch from obra/superpowers
 */

const { TDDWorkflow } = require('./tdd-workflow');
const { SubagentDispatch } = require('./subagent-dispatch');

module.exports = {
  TDDWorkflow,
  SubagentDispatch,
};
