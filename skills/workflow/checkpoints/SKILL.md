# checkpoints

Milestone-based progress tracking with checkpoints

## When to use
Use this skill when you need to milestone-based progress tracking with checkpoints.

## Key method
`constructor(input)` — runs the checkpoints analysis and returns a structured result.

## Example
```js
const Checkpoints = require('./index.js');
const skill = new Checkpoints();
const result = skill.constructor(input);
console.log(result);
```
