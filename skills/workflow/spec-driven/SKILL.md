# spec-driven

Keep AI aligned with project intent using spec-driven development

## When to use

Use this skill when you need to keep ai aligned with project intent using spec-driven development.

## Key method

`constructor(input)` — runs the spec-driven analysis and returns a structured result.

## Example

```js
const SpecDriven = require('./index.js');
const skill = new SpecDriven();
const result = skill.constructor(input);
console.log(result);
```
