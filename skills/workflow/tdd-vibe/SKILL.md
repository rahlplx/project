# tdd-vibe

Simplified TDD workflow for vibe coders — test-first without the jargon

## When to use

Use this skill when you need to simplified tdd workflow for vibe coders — test-first without the jargon.

## Key method

`constructor(input)` — runs the tdd-vibe analysis and returns a structured result.

## Example

```js
const TDDVibe = require('./index.js');
const skill = new TDDVibe();
const result = skill.constructor(input);
console.log(result);
```
