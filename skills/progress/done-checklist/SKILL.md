# done-checklist

Production-ready checklist — tells non-coders if their project is ready to ship

## When to use

Use this skill when you need to production-ready checklist — tells non-coders if their project is ready to ship.

## Key method

`constructor(input)` — runs the done-checklist analysis and returns a structured result.

## Example

```js
const DoneChecklist = require('./index.js');
const skill = new DoneChecklist();
const result = skill.constructor(input);
console.log(result);
```
