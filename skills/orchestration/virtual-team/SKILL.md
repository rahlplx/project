# virtual-team

Role-based specialist agents for different perspectives

## When to use

Use this skill when you need to role-based specialist agents for different perspectives.

## Key method

`constructor(input)` — runs the virtual-team analysis and returns a structured result.

## Example

```js
const VirtualTeam = require('./index.js');
const skill = new VirtualTeam();
const result = skill.constructor(input);
console.log(result);
```
