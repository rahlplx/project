# vibe-review

Non-technical code review — plain English feedback for vibe coders

## When to use

Use this skill when you need to non-technical code review — plain english feedback for vibe coders.

## Key method

`constructor(input)` — runs the vibe-review analysis and returns a structured result.

## Example

```js
const VibeReview = require('./index.js');
const skill = new VibeReview();
const result = skill.constructor(input);
console.log(result);
```
